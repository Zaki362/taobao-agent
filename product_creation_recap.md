# SceneCart AI / AutoPrep AI 项目复盘

## 1. 整体产品解决方案

### 1.1 这个产品要解决什么问题

这个产品最初瞄准的是一个非常具体、但又足够典型的消费决策问题：**用户并不是缺“商品搜索工具”，而是缺“围绕一个真实生活场景做分阶段购物决策的 Agent”**。

以“新车选购 / 新车用品首购”为例，用户面对的真实问题不是“我要搜一个手机支架”，而是：

- 我刚提车，第一阶段到底该买什么
- 有限预算下哪些是必须先补齐的
- 哪些属于体验升级，哪些可以晚点再买
- 每个模块里具体选什么商品更合适
- 最后怎么把这些商品组织成一套可执行的购物方案

传统电商平台解决的是“已知目标商品”的查找问题，但并不解决“任务型购物”的决策组织问题。用户往往需要自己在脑中完成以下复杂动作：

1. 理解当前场景
2. 拆分成若干购物模块
3. 给模块排优先级
4. 做预算分配
5. 为每个模块找到候选商品
6. 再进入购物执行

这个产品的目标，就是把用户脑中的这套隐性决策过程产品化、结构化、显性化。

### 1.2 最终产品方案是什么

最终形成的方案不是一个聊天机器人，也不是一个单页商品推荐页，而是一个**场景化购物 Agent 产品**：

- 用户输入场景需求
- 系统先做场景理解
- 再基于场景模板生成购物规划
- 再按模块搜索和整理候选商品
- 最后支持进入下单前确认和购物执行

整个产品采用统一的分阶段流程：

1. 场景入口 / 需求输入
2. 场景理解
3. 场景确认
4. 规划生成
5. 规划确认
6. 商品搜索
7. 推荐结果查看
8. 快捷调整 / 局部重算
9. 购物车确认 / 下单前清单

这让产品从一开始就不是“帮你搜商品”，而是“帮你完成一次场景任务”。

### 1.3 用户完整使用路径

当前产品下，用户的完整使用路径大致是：

1. 首页选择“新车选购”场景
2. 输入需求，例如：
   - 刚提新能源车，预算 1500，希望优先买最实用的新车用品，不考虑装饰类
3. 系统进入需求理解阶段，生成结构化 Scene Brief
4. 用户在确认页核对并可手动修改：
   - 车型
   - 预算
   - 偏好
   - 阶段
   - 排除项
   - 已有物品
5. 系统基于模板 + DeepSeek 补充，生成购物规划
6. 用户确认模块优先级与预算分配
7. 系统串行搜索各模块商品
8. 用户在推荐页查看模块化结果，并可进行快捷调整
9. 用户把商品加入购物车
10. 若真实加购失败，则回退到产品内 demo 购物车，保证演示完整性
11. 用户进入下单前确认页查看清单、总价与商品信息

产品体验上强调的是：**逐步理解、逐步确认、逐步执行**，而不是一上来把结果全部摊开。

---

## 2. Agent 层设计（重点）

### 2.1 Agent 层在整个系统中的位置

这个项目最关键的部分，不是前端页面，也不是 DeepSeek 接口本身，而是位于中间的 **Agent orchestration 层**。

这个层的作用，是把以下几类能力组织起来：

- 用户场景输入
- 结构化理解
- 场景模板
- DeepSeek 补充能力
- 商品搜索逻辑
- 淘宝 skill / MCP / CLI 工具
- 服务端 session / 执行状态 / 工具日志

也就是说，产品不是“前端直接调 LLM”，也不是“前端直接搜商品”，而是：

**前端 -> Agent 编排层 -> DeepSeek / 模板 / 工具执行层**

Agent 层的职责，是决定在当前阶段：

- 是否应该调用模型
- 调哪个能力
- 是否该搜索
- 搜哪个模块
- 当前结果写回哪里
- 哪些内容展示给前端

### 2.2 Agent、DeepSeek、后端规则、场景模板、MCP 工具层分别负责什么

#### Agent / Orchestrator

Agent 主编排层承担的是“流程控制与状态汇总”的职责。

它负责：

- 持有 session state
- 组织多阶段 workflow
- 在需求理解阶段调用 parse_scene
- 在规划阶段加载模板并调用 personalize_template
- 在 refine 阶段调用 refine_plan
- 在搜索阶段决定按哪个模块依次执行搜索
- 汇总模块商品候选池
- 维护 selected_items / tool_logs / current_scene_label

也就是说，Agent 层是整个系统的“大脑”，它不负责生成所有内容，但负责决定每一步怎么走。

#### DeepSeek

DeepSeek 在这个系统里不是“全能规划器”，而是一个**受约束的结构化补充器**。

它只承担四类职责：

1. `parse_scene`
2. `personalize_template`
3. `refine_plan`
4. `explain_product_fit`

这样做的原因是：

- 模型适合做不完全信息补足、抽象理解和结构化调整
- 模型不适合直接承担所有工具编排和业务决策
- 一旦让模型直接“自由决定全部逻辑”，就会失控、不可测、不可维护

所以模型被限制在“理解、补充、微调、解释”这四类任务里。

#### 后端规则层

后端规则层承担的是“确定性业务逻辑”。

例如：

- session 如何生成
- priority 如何归一化
- module_candidates 如何写入
- refine 后哪些模块要清空旧结果
- 搜索阶段如何串行执行
- 购物车失败时如何回退到 demo cart

这些内容不应该交给模型，因为它们需要可控、稳定、可测试。

#### 场景模板层

模板层承担的是“结构稳定性”。

它决定：

- 有哪些模块
- 模块默认预算占比
- 默认优先级
- typical item types

例如新车场景下：

- 安全必需
- 清洁维护
- 车内实用
- 收纳整理
- 舒适升级
- 装饰氛围

模板的价值是：先保证结构正确，再让 LLM 做补充，而不是让模型从零编一个完整方案。

#### MCP / skill / 工具层

工具层负责的是“真实世界执行”。

设计上经历了几轮演化：

1. 最初设计成标准 MCP adapter
2. 尝试通过本地 bridge 连接 taobao-native
3. 后来尝试通过 Qoder skill / qodercli 调淘宝 skill
4. 最终发现搜索链路可用，但商品详情页 / 加购存在淘宝客户端会话问题
5. 所以当前采用保守策略：
   - 搜索真实执行
   - 加购先尝试真实执行
   - 失败则回退到产品内 demo 购物车

这个层负责的不是“思考”，而是“把规划结果变成真实动作”。

### 2.3 Context 怎么管理

整个项目的上下文不是靠前端临时拼接，而是以服务端 `SessionState` 为中心统一管理。

核心状态对象包括：

- `session_id`
- `raw_input`
- `scene_brief`
- `base_template`
- `shopping_plan`
- `module_candidates`
- `selected_items`
- `tool_logs`
- `hosted_tasks`
- `execution_mode`
- `permissions_scope`
- `deepseek_status`
- `mcp_status`
- `current_scene_label`
- `last_action`

这样做的好处是：

- 前后端对当前状态有统一认知
- 快捷调整时能在原上下文上重算
- 搜索结果与购物车状态可以持续积累
- 后端执行台可以读取同一份状态进行展示

### 2.4 skill / MCP 工具怎么调用

工具调用的抽象方式，是统一使用 adapter / executor 模式。

逻辑上支持的动作包括：

- 搜索商品
- 打开商品详情
- 读取商品信息
- 加入购物车

但在实际实现中，为了兼容不同执行方式，后端做了统一封装：

- `lib/mcp/client.ts`
- `lib/mcp/executor.ts`
- `lib/mcp/qoder.ts`
- `lib/mcp/mock.ts`

产品层调用的不是“某个具体 CLI”，而是统一的工具名，例如：

- `search_taobao_products`
- `open_product_detail`
- `extract_product_info`
- `add_to_cart`

然后由底层 adapter 决定：

- 是走 Qoder
- 是走 mock
- 还是走其他 bridge

### 2.5 为什么采用“模板 + LLM 补充”，而不是让模型自由乱生成

这是产品和工程都非常关键的一次决策。

如果完全让模型自由生成，会出现几个问题：

1. 结构不稳定  
   同一个场景多次生成的模块可能完全不一致。

2. 难以评估对错  
   模型可能会漏掉真正高频刚需，也可能凭空补出奇怪模块。

3. 难以后续维护  
   一旦想扩展到多个场景，完全自由生成的结构几乎无法复用。

4. 很难和后续工具调用衔接  
   工具链更适合消费结构化模块，而不是任意文本规划。

所以最后采用了：

**模板提供结构骨架，LLM 只做个性化调整**

这样既保留了：

- 稳定性
- 可测性
- 可维护性

又保留了：

- 场景适配能力
- 个性化能力
- 用户约束理解能力

### 2.6 为什么模型只负责 parse_scene / personalize_template / refine_plan / explain_product_fit

因为这四类任务最适合模型：

#### parse_scene

输入是自然语言，输出是结构化 Scene Brief。  
这是典型的模型擅长任务。

#### personalize_template

输入是稳定模板，输出是对优先级、预算分配、模块裁剪的微调。  
模型适合做这种“半结构化补充”。

#### refine_plan

输入是快捷操作和现有 scene brief，输出是小范围重算。  
这很适合模型根据用户偏好做轻量调整。

#### explain_product_fit

输入是商品标题和模块信息，输出是简洁推荐理由。  
这也是模型擅长的解释类任务。

而工具调用、任务调度、session 管理、结果落库等事情不适合模型负责，因为：

- 需要确定性
- 需要可控
- 需要可回放
- 需要工程稳定性

---

## 3. 遇到的问题与解决方法（重要）

这一部分是整个项目最值得讲的内容。因为产品并不是“一次成型”，而是经历了大量方向调整、边界收缩和技术取舍。

### 3.1 产品方向问题

#### 问题 1：最初版本更像“购物控制台”，不像 Agent 产品

最早页面是三栏大屏式结构，一上来就把：

- Scene Brief
- 模板
- 模块
- 商品推荐
- MCP 状态
- 执行日志

全部铺开。

这导致几个问题：

- 首屏信息过载
- 用户不知道先看哪里
- 没有“Agent 在一步步帮助你完成任务”的过程感
- 看起来更像后台系统，而不是 AI Native 产品

#### 为什么这是问题

这个产品本质上要讲的是“渐进式任务决策”，而不是“全量信息展示”。  
如果没有过程感，用户不会觉得这是一个 Agent，只会觉得这是个一次性算结果的页面。

#### 解决方案

后来把交互彻底重构成了分阶段单主视图：

- landing
- input_requirement
- parsing
- confirm_scene
- planning
- confirm_plan
- searching
- review_results
- refining
- cart_review

并强调：

- 每一步都有清晰任务
- 每一步结束都让用户确认
- 搜索阶段和推荐阶段拆开

#### 当前状态

这个问题已经基本解决。  
当前产品的阶段感、确认感和流程感已经比最早版本强很多。

#### 遗留问题

某些次级页面，例如购物车确认页、执行台，还可以进一步做得更像真实成熟产品，而不是“功能完整但产品味略弱”。

---

#### 问题 2：产品定位一开始过于绑定“新车用品首购”

项目最初完全聚焦新车场景，这在打磨首个 demo 时是正确的，但很容易让人误以为这只是一个“新车用品工具”。

#### 为什么这是问题

如果定位被锁死在“新车”，那么底层能力的泛化价值就不明显：

- 场景理解
- 模板规划
- 模块化搜索
- 快捷调整
- 购物执行

这些其实都是跨场景通用能力。

#### 解决方案

后来做了两层调整：

1. 首页定位升级为“场景化购物 Agent”
2. 开始抽象 scenario config 体系，为未来多场景扩展做准备

#### 当前状态

定位层已经完成抽象，首页层面具备了多场景产品的方向。  
但代码层面的多场景完整泛化尚未完全收尾，当前稳定可用版本仍以“新车选购”为主。

#### 遗留问题

多场景配置层已经部分建立，但为了保证当前产品稳定，暂时没有把前端完全切换到全场景配置驱动版本。

---

### 3.2 Agent / 模型边界问题

#### 问题 1：模型边界一开始不够清晰

在方案讨论初期，很容易产生一种冲动：  
“既然有大模型，那是不是可以让模型直接完成场景理解、规划、推荐、工具调用决策、结果汇总的全部流程？”

#### 为什么这是问题

这会导致：

- 流程失控
- 工具调用不可控
- 输出结构不稳定
- 很难做 UI 和状态管理
- 很难做后续多场景扩展

#### 解决方案

后来明确分层：

- 模型只负责：
  - parse_scene
  - personalize_template
  - refine_plan
  - explain_product_fit
- Agent / 后端负责：
  - 模板装配
  - 工作流推进
  - 搜索调度
  - 执行日志
  - 商品候选池汇总

#### 当前状态

这个问题基本已经解决。  
当前模型边界清晰，是整个架构最稳定的一部分。

#### 遗留问题

`explain_product_fit` 在实际性能上被削弱过一轮，因为搜索阶段如果每个商品都额外走模型，会严重拖慢整体速度。  
所以当前在部分路径上使用了更轻量的本地快速生成文案，而不是每个商品都走 DeepSeek。

---

#### 问题 2：为什么不是纯聊天式产品

一个典型分歧是：  
“既然有 LLM，是不是整个产品做成聊天界面就够了？”

#### 为什么这是问题

聊天适合开放式探索，不适合购物任务型决策的结构化流程。  
纯聊天会让用户面临：

- 信息回看困难
- 状态不可见
- 快捷调整效率低
- 商品和模块结构弱化

#### 解决方案

明确采用：

- 主流程非聊天
- 快捷操作按钮驱动
- 结构化确认页面
- 模块化结果页

#### 当前状态

这个方向已经非常明确，并且是产品最有辨识度的部分。

---

### 3.3 淘宝 MCP / skill 接入问题

#### 问题 1：本地 bridge 和 taobao-native 调用链不稳定

项目中最复杂的问题，几乎都出在淘宝执行链路。

一开始尝试的是：

- 本地 bridge
- `taobao-native`
- live / mock adapter

目标是让产品真正执行：

- 搜索
- 打开详情
- 提取商品
- 加入购物车

#### 为什么这是问题

理论上工具链是通的，但在真实环境里出现了非常多问题：

- bridge 可连但调用异常
- 工具执行层未就绪
- 权限状态不一致
- 客户端授权不透明
- 健康检查成功但真实调用失败

#### 解决方案

后来尝试了多条路径：

1. live-first MCP adapter
2. taobao-native bridge
3. Qoder skill / qodercli
4. Codex 宿主代理

最后得出的结论是：

- 搜索阶段相对可用
- 商品详情页和加购阶段风险最大

#### 当前状态

当前搜索链路相对稳定，是真实可用的。  
加购链路则采用“真实尝试 + 失败回退 demo cart”的保守策略。

#### 遗留问题

淘宝客户端的真实行为有明显不可控性，目前仍然不是完全可靠的生产级能力。

---

#### 问题 2：Qoder skill 能搜，但 stdout 经常不稳定

在接入 Qoder 时发现一个很诡异的问题：

- skill 实际执行可能成功了
- 工作区里甚至已经有结果文件
- 但 `qodercli` 最终没有把 JSON 稳定输出到 stdout

于是前端看到的是：

- `Qoder CLI 未返回内容`
- `max turns reached`
- `执行超时`

#### 为什么这是问题

如果产品完全依赖 Qoder stdout，则：

- 工具实际成功与否无法准确感知
- 超时和失败混在一起
- 用户体验不稳定

#### 解决方案

后来做了多层缓解：

- 收紧 prompt
- 限制 allowed tools
- 增加超时兜底
- 尝试读取结果文件作为 fallback
- 把搜索串行化，减少同时调用过多模块

#### 当前状态

搜索链路已经被打磨到“基本能顺畅跑完多个模块”的程度。

#### 遗留问题

Qoder CLI 不是一个为这个产品量身设计的稳定 API，所以仍然存在偶发超时和不确定性。

---

#### 问题 3：商品详情页导航会触发退出登录

这是整个项目里最棘手、最反直觉的问题之一。

现象是：

- 搜索正常
- 首页 / 购物车页导航正常
- 但一旦调用 `navigate_to_url` 打开商品详情页，淘宝客户端会立即跳到登录页
- 甚至会连带手机端一起掉登录

#### 为什么这是问题

因为：

- `get_product_skus`
- `add_to_cart`

这类动作本质上都依赖商品详情页或订单权限。  
也就是说，只要详情页导航不稳定，后续加购就很难可靠。

#### 解决方案

做过很多轮尝试：

- 改链接格式
- 回退链接规范化
- 调整预检方式
- 收紧 prompt
- 减少主动打开详情页

最终结论是：

这不是简单代码 bug，而是淘宝客户端 / 工具执行层在当前环境下的真实限制。

因此最后采用的策略是：

- 搜索阶段不自动打开商品详情页
- 商品卡只返回搜索摘要
- 加购先尝试真实执行
- 失败则回退到产品内 demo cart

#### 当前状态

这个问题没有被彻底解决，只是被产品化处理了。

#### 遗留问题

如果要做成生产级产品，必须换一种更稳定的淘宝执行接口，而不是继续依赖当前这条详情页不稳定的调用链。

---

### 3.4 前端交互和页面设计问题

#### 问题 1：首页和结果页最初原型感太强

早期页面存在：

- 大量边框卡片但缺乏主次
- 留白不一致
- 组件风格不统一
- 页面像控制台多过像购物产品

#### 为什么这是问题

产品虽然有功能，但缺乏“成熟度”和“可信感”，很难作为面试项目体现产品审美与产品化能力。

#### 解决方案

后来做了系统级 UI 重构：

- 统一设计 token
- 统一圆角、边框、间距、阴影
- 强化商品卡电商感
- 弱化模型和日志信息
- 让主视图始终围绕“当前任务”

#### 当前状态

当前已经明显比最早版本更接近成熟产品，而不是 demo 原型。

#### 遗留问题

购物车确认页、执行台等页面还可以继续强化细节层级与电商感。

---

#### 问题 2：快捷调整后的体验一度很差

早期快捷调整点击后，会直接自动重算并立刻进入搜索，造成：

- 等待时间长
- 用户失去掌控感
- 看起来像卡住

#### 解决方案

后来改成：

- 快捷调整只先重算规划
- 回退到确认规划页
- 用户确认后再点击开始搜索

#### 当前状态

这个体验已经明显更顺，用户也更能理解“调整后重新规划”的逻辑。

---

### 3.5 工程实现问题

#### 问题 1：前端和状态机一度不同步

在不断重构产品过程中，曾出现过多次典型工程问题：

- session not found
- parse route / plan route 内部不一致
- 页面刷新后状态丢失
- 从执行台返回后回到首页重新开始

#### 为什么这是问题

因为这个产品并不是单页静态展示，而是一个多阶段状态机系统。  
一旦状态管理不严谨，整个体验就会崩。

#### 解决方案

做了几轮修正：

- 服务端统一 session store
- 前端本地快照恢复
- hosted console 返回当前进度
- 开新一轮购物时清掉旧 resume banner

#### 当前状态

状态恢复和当前进度返回已经比较稳定。

#### 遗留问题

多场景泛化如果重新推进，必须更加谨慎，否则会再次引入“半截类型变更污染当前流程”的问题。

---

## 4. 产品如何泛化场景

这一部分是当前产品从“单场景 demo”走向“多场景框架”的核心思路。

### 4.1 核心能力层：跨场景通用

无论是什么购物场景，真正可复用的是这套核心能力：

- 需求输入
- 结构化场景理解
- 确认需求
- 模板规划
- 确认规划
- 模块化商品搜索
- 推荐结果查看
- 快捷调整
- 购物确认

也就是说，**workflow 是通用的**。

通用能力还包括：

- session/context 管理
- DeepSeek 调用边界
- 模块优先级与预算分配
- MCP / skill 搜索工具调用
- 推荐结果模块化展示

### 4.2 场景模板层：按场景配置

真正需要因场景而变的，是模板和文案，而不是流程本身。

按场景配置的内容包括：

- 首页场景标题和简介
- 输入提示文案
- 示例需求
- Scene Brief 字段标签和选项集
- 基础模板模块
- 模块说明文字
- 推荐页模块 tab 名称
- 推荐理由文案风格
- 风险提示文案
- 快捷操作按钮

### 4.3 从单场景 demo 到场景配置驱动的演进

最初版本是典型单场景：

- 页面、字段、模块、快捷按钮都写死成“新车”

后面开始做的抽象是：

1. 保持统一 workflow
2. 引入 `ScenarioConfig`
3. 把：
   - 文案
   - 字段
   - 模块模板
   - 快捷操作
   - 推荐风格
   放到配置层

也就是说，产品的演进方向不是“复制五套页面”，而是：

**统一流程 + 场景配置驱动**

当前这层架构已经起了头，但为了不影响当前稳定版本，最终没有在这一轮完全切换上线。

---

## 5. 总体实现过程

### 5.1 选题与方向探索

最早的方向并不是“做一个普通购物推荐页”，而是试图寻找一个足够具体、又足够能体现 Agent 价值的消费场景。

“新车用品首购”之所以被选中，是因为它同时具备：

- 任务明确
- 决策复杂
- 商品模块清晰
- 预算约束明显
- 用户真实会纠结“先买什么、后买什么”

这使它非常适合作为 Agent 化产品的第一个 demo。

### 5.2 场景化购物方案形成

最初方案可能更接近“搜商品 + 推荐”，但后来逐渐明确：

- 这个产品的价值不在于单次推荐
- 而在于把复杂决策过程分解出来

于是产品从“搜索工具”演进成：

**场景理解 -> 规划 -> 搜索 -> 调整 -> 下单前确认**

### 5.3 Agent 和技术架构确定

随着产品方案变清晰，技术架构也逐渐收敛成：

- 前端：Next.js App Router + React + TS + Tailwind
- 后端：API routes + in-memory/local session state
- Agent 层：orchestrator / scene / planner / refiner / product-matcher / cart
- 模型层：DeepSeek
- 工具层：淘宝 skill / MCP / qodercli / mock

这一阶段最重要的决策，是明确模型和工具的职责边界。

### 5.4 MCP / DeepSeek 方案讨论

这一阶段的关键分歧点包括：

- 模型是不是应该直接决定所有工具调用
- 淘宝到底是走 bridge、skill、qoder 还是宿主代理
- 搜索是不是要自动打开详情页
- 加购是不是一定要做成真实执行

最终形成的方案是：

- 模型收缩职责
- 搜索优先保证稳定
- 加购保守处理
- 工具真实能力不稳定时，产品层提供 demo fallback

### 5.5 前端从控制台式页面改成 Agent 分步流程

这一步是产品体验上的重大转折。

原本的页面虽然信息多，但没有明确过程。  
后来重构成统一阶段式流程后，产品的“Agent 感”才真正建立起来。

这一步体现的不是简单 UI 调整，而是：

- 用户心智路径重构
- 状态机重构
- 页面职责拆分

### 5.6 从单场景扩展到泛场景

当新车场景逐渐成熟后，下一步自然是思考：

- 这个流程是不是只适合新车？
- 底层能力能否迁移到露营、房间装饰、宿舍入学、搬家置办？

这就推动了 scenario config 抽象的开始。

虽然这一步在代码上还没有全部收尾，但它已经明确了产品未来的真正方向：  
不是“新车工具”，而是“场景化购物 Agent 框架”。

### 5.7 UI 和产品感优化

随着流程逐渐稳定，最后一轮重要工作是：

- 统一视觉系统
- 提升电商感
- 强化商品卡质感
- 弱化后台感
- 让执行台也更产品化

这一步让项目从“能跑的技术 demo”，变成“可以拿来展示产品思考和审美能力的作品”。

---

## 6. 后续可改进部分（必须严格批判）

这一部分必须诚实，因为当前产品虽然已经有完整形态，但离真正成熟产品还有不小距离。

### 6.1 产品层

#### 最大不足

当前产品的流程已经清晰，但仍然偏“专家视角设计”，对于普通用户来说：

- 模块规划页信息量仍然较高
- 推荐理由和风险提示的价值层级还不够明确
- 购物车确认页和真实电商结算页之间还有明显落差

#### 理想改进方向

- 进一步压缩非核心信息
- 让每一步只有一个真正主任务
- 推荐页加入更清晰的“为什么先买这个”
- 下单确认页做成更接近真实结算前清单，而不是商品汇总卡片

### 6.2 Agent 层

#### 最大不足

当前 Agent 虽然已经有明确分层，但仍偏“流程编排器”，还不够像真正具备强决策能力的复杂 Agent。

例如：

- 模块搜索还是按固定串行执行
- 对失败模块的恢复策略有限
- 对不同场景的策略迁移还不够成熟

#### 理想改进方向

- 引入更细粒度的模块级状态机
- 增加失败恢复和 retry 策略
- 对不同场景建立更明确的策略规则层

### 6.3 模型层

#### 最大不足

当前模型被合理地限制了边界，但也因此出现两个问题：

1. 在某些地方模型参与过少，导致解释层略显模板化
2. 在需要速度时，不得不削减模型参与度，牺牲了一部分“智能感”

#### 理想改进方向

- 做更细粒度的模型缓存和复用
- 让 `explain_product_fit` 在不拖慢搜索的前提下恢复更多个性化
- 针对不同场景定义更成熟的 schema 和 few-shot prompt

### 6.4 MCP / skill / 工具层

#### 最大不足

这是当前产品最不成熟的部分，也是离生产化最远的部分。

核心问题包括：

- Qoder / skill / taobao-native 并不是稳定的产品级 API
- 商品详情页导航会触发退出登录
- 加购链路不可靠
- stdout / tool 结果不稳定

#### 理想改进方向

- 换成真正稳定、权限清晰、可控的淘宝执行接口
- 引入专用的购物车查询 / 删除 / SKU 选择能力
- 减少对页面自动化的依赖
- 把执行层从“实验性连接”升级为“可观测的稳定服务”

### 6.5 前端交互层

#### 最大不足

虽然现在已经比早期好很多，但前端仍然存在几个问题：

- 某些信息块的主次还可进一步优化
- 推荐页和下单页的切换还不够丝滑
- 执行台和主产品之间虽然风格统一了，但协同感还能更强

#### 理想改进方向

- 进一步优化动效与反馈节奏
- 对搜索中 / 重算中 / 加购中的状态做更细腻设计
- 提升推荐页到下单页的心智连续性

### 6.6 工程实现层

#### 最大不足

当前工程实现虽然已经能支撑 demo，但离长期演进还有明显差距：

- 状态管理仍偏集中在单个大组件
- 场景配置泛化只完成了一部分
- 有较多产品快速迭代留下的历史包袱
- MCP / Qoder / hosted 等执行方案并存，导致复杂度偏高

#### 理想改进方向

- 拆分更清晰的页面组件和 hooks
- 真正完成 scenario config 驱动改造
- 清理历史执行路径，保留更明确的主执行方案
- 增加更系统的类型约束和集成测试

---

## 结语

这个项目最有价值的地方，不是“做了一个能跑的购物页面”，而是经历了一个完整的产品创作过程：

- 从单点问题识别开始
- 到产品范式形成
- 到 Agent 架构和模型边界收敛
- 到工具层现实约束暴露
- 再到前端体验、交互流程、UI 体系不断重构

它并不是一条线性完成的过程，而是一系列持续的取舍：

- 什么该交给模型
- 什么必须由规则控制
- 哪些工具能力应该真实执行
- 哪些地方要为了稳定性做产品化妥协
- 如何把技术限制转化成仍然可演示、可理解、可汇报的产品方案

从这个角度看，这个项目的意义不仅在于“实现了什么”，更在于：

**它真实体现了一个 AI 产品从 0 到 1 的设计、重构、收缩、泛化和落地过程。**
